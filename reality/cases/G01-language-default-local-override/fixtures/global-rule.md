# Managed global language default

Reply in Chinese unless another language is explicitly requested. Keep code, configuration, and technical identifiers in English.

This is a stable cross-task default, not a rule that forbids local-scope language overrides.
