# Authorized local-scope override excerpt

In a real MCM paper task, the user asked to improve fifteen raw tables and explicitly required the table-facing deliverable to be in English because the MCM paper is written in English.

The override applies to that MCM deliverable. It does not request replacing the user's global language default for later unrelated tasks.
