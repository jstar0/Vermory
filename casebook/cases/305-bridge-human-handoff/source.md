# Bridge Workspace To Human Handoff

The `inventory-audit` workspace needs a human handoff for the next developer.

The handoff must include the active goal, next step, rejected direction, and risk.

The active goal is to reconcile warehouse counts against supplier invoices. The next step is to add a mismatch review table.

The rejected direction is automatic invoice mutation. The main risk is stale supplier CSV exports.

