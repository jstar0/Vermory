# Multi Stage Product Design Workspace

`campus-pass` is a product design workspace for a campus visitor pass system.

The project moved through positioning, PRD, technical approach, and prototype planning.

The current stage is prototype planning for QR-code check-in. A previous idea to use facial recognition was rejected because it is too privacy-sensitive for the first release.

The current constraint is to support offline guard verification using cached pass records.

