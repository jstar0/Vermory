# Conversation Job Search

Maya is using chat to manage a job search across several companies.

The active role target is backend platform engineer, with compensation negotiation tracked separately from interview prep.

`Northstar Labs` is still active after a system design interview. `Harbor AI` was rejected after the recruiter clarified it was a mostly frontend role.

A separate thread about a friend's resume should not merge into Maya's active job search.

