# Bridge Workspace To Report

The `ContextMesh` workspace needs to export a discussion-ready project brief for a competition report.

The export must preserve the current product model: workspace continuity, conversation continuity, global defaults, and bridge actions.

The report should not include raw implementation scratch notes, provider API keys, or old MemOS dependency framing.

The target audience is a review committee, not a coding agent.

