# Bridge Workspace Rebind

The historical `ContextMesh` workspace moved from a local scratch path to `/workspaces/vermory`.

The adopt/rebind action should preserve the same continuity ID rather than creating a new workspace line.

The old scratch path may still exist but should be treated as a historical alias.

Any rebind should leave audit evidence because wrong workspace binding is high risk.
